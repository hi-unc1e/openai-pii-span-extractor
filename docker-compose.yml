services:
  pii-span-extractor:
    image: ghcr.io/hi-unc1e/pii-span-extractor:latest
    container_name: pii-span-extractor
    ports:
      - "8000:8000"
    environment:
      - OPF_DEVICE=cpu
      - OPF_OUTPUT_MODE=typed
    restart: unless-stopped
